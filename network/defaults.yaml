{% load_yaml as rawmap %}
Debian:
  interface_defaults:
    enabled: True
    proto: dhcp
    type: eth
  resolver:
    path: /etc/resolv.conf
  default_interfaces:
    - name: lo
      proto: loopback
      type: eth
{% endload %}
